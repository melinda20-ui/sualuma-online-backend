import "./flowmind.css";

export const metadata = {
  title: "FlowMind — Seu GPS Mental",
  description: "Plataforma de produtividade para solopreneurs.",
};

export default function FlowMindLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
